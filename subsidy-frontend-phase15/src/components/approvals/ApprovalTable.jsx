function formatDate(value) { if (!value) return '—'; const date = new Date(value); return Number.isNaN(date.getTime()) ? value : date.toLocaleString('en-IN'); }
export default function ApprovalTable({ approvals, updatingId, onUpdate }) {
  if (!approvals.length) return <div className="empty-state"><strong>No approval records found</strong><span>Approval records will appear here after decisions are submitted.</span></div>;
  return <div className="table-wrapper"><table className="data-table"><thead><tr><th>ID</th><th>Application</th><th>Approved By</th><th>Status</th><th>Amount</th><th>Decision Date</th><th>Remarks</th><th>Action</th></tr></thead><tbody>{approvals.map((approval) => <tr key={approval.id}><td>#{approval.id}</td><td>#{approval.applicationId}</td><td>#{approval.approvedById}</td><td><span className={`status-badge status-${String(approval.status).toLowerCase()}`}>{approval.status}</span></td><td>₹{Number(approval.approvedAmount || 0).toLocaleString('en-IN')}</td><td>{formatDate(approval.decisionDate)}</td><td>{approval.remarks || '—'}</td><td><select value={approval.status} onChange={(event) => onUpdate(approval, event.target.value)} disabled={updatingId === approval.id}><option value="APPROVED">APPROVED</option><option value="REJECTED">REJECTED</option></select></td></tr>)}</tbody></table></div>;
}
